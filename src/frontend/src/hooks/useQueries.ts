// This file is intentionally minimal as the app requires no backend queries
// All functionality is handled client-side

export function useQueries() {
  // No backend queries needed for this app
  return {};
}
